node_modules/
build/
vendor/

# Distribution artifact (built by `wp-scripts plugin-zip`)
smart-booking.zip

.DS_Store
.vscode/
.idea/
# .claude は無視のまま、サブエージェント定義 (.claude/agents/) だけ追跡する。
# 親ディレクトリ全体を無視すると中身を !再包含できないため、`.claude/*` にしてから除外解除する。
.claude/*
!.claude/agents/
!.claude/agents/**
*.log

.wp-env.override.json

# Playwright
test-results/
playwright-report/
.playwright/
tests/e2e/.db-snapshot.json
tests/e2e/screenshots/

# 公式サイト ヘルプ用スクリーンショット（生成物・コミットしない。README は追跡する）
docs/website-screenshots/**/*.png

# 撮影時の DOM テキストダンプ（どのカットに何の文字が写っているかを grep するための生成物）
screenshot-text/

# API credentials (絶対にコミットしない)
credentials/