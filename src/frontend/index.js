/**
 * Smart Booking フロント予約フォーム React エントリーポイント。
 *
 * フェーズ1 では空のマウントのみ。フェーズ3 で予約フォームコンポーネントを追加する。
 */
// eslint-disable-next-line no-unused-vars
const container = document.getElementById( 'smart-booking-app' );
// マウント処理はフェーズ3で追加する。
