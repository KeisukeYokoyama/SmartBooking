# Smart Booking v0.2.3 バグ台帳（正本）

最終更新: 2026-07-12
対象バージョン: 0.2.3（公開済み 0.2.2 の不具合修正）
運用: planner 主導のバグ修正ループ。各バグは「再現ファースト（Red）→ 修正 → 回帰ゲート Green」で閉じる。

コード調査は GitHub 正本（`KeisukeYokoyama/SmartBooking`, main）に対して実施。行番号は調査時点のもの。

---

## BUG-1 / BUG-2（同一根因）: スケジュールコピーが店舗・担当者を無視する

- **重大度**: 🔴 Critical（データ損失・サイレント無登録）
- **報告症状**:
  - 不具合1「複数店舗があり担当者が複数の場合、スケジュール登録が不安定」
  - 不具合2「登録したはずなのにスケジュールが登録されていない」
- **該当**: `includes/rest/class-rest-schedules.php` の `copy_schedules()`

### 根本原因
フロント（`src/admin/pages/schedule/ScheduleCopyModal.jsx`）は `store_id` / `staff_id` を正しく送信している。しかしバックエンドはそれを**コピー元の抽出にしか使わず**、既存チェック・スキップ・上書き削除を **`schedule_date`（日付）だけ**で全店舗・全担当者をまたいで実行している。

- 既存チェック: `SELECT COUNT(*) ... WHERE schedule_date = %s`（店舗/担当者の絞り込みなし）
- 上書き削除: `DELETE ... WHERE schedule_date = %s AND booked_count = 0`（同上）
- `overwritten_total += $existing` も全店舗合算で誤カウント。

### 再現条件（「2つ以上の店舗/担当者グループが同じコピー先日付に枠を持つ」とき）
- **上書きOFF でコピー**: コピー先の日付に*他の*店舗/担当者の枠が1件でもあると、その日全体が「スキップ」判定になり、意図した店舗のコピーが**1件も入らない**。UI は「N件スキップ」と出すが見落としやすい → 体感「コピーしたのに登録されない」（不具合2）。
- **上書きON でコピー**: その日の*他の*店舗/担当者の未予約枠まで `DELETE` され、再挿入されるのはコピー元の店舗/担当者だけ → **他店舗・他担当者の既存スケジュールが消える**（不具合1の「不安定」＝データ損失）。単一店舗でも担当者が2人以上いれば発生。

### 修正方針
1. `copy_schedules()` の既存チェック・上書き DELETE・カウントに、コピー元と同じ `store_id` / `staff_id` の絞り込みを**必ず含める**（＝コピーの作用範囲を「その店舗×担当者」に閉じる）。
2. 上書きは日付一括 DELETE ではなく、対象 `(store_id, staff_id, schedule_date)` かつ `booked_count = 0` の枠に限定。予約済み枠は従来どおり保護。
3. `overwritten` / `skipped` カウントを対象スコープ基準に修正。
4. 併せて BUG-4（UNIQUE 制約）を入れると、コピーの多重実行でも重複が構造的に防げる。

### 検証（logic-evaluator）
- 店舗A/B・担当者2名・重なる日付を作る隔離ゲート。
- Red 例: 「店舗Bの枠を、店舗Aの枠がある日付へ上書きコピー → 店舗Aの枠が消えない」ことを assert（修正前は落ちる）。
- Red 例: 「上書きOFF で店舗Aの枠がある日付へ店舗Bをコピー → 店舗Bの枠がちゃんと作成される」。
- 回帰: 単一店舗・単一担当者のコピーが従来どおり動く。予約済み枠が保護される。

---

## BUG-3: 予約通知メールが届かない

- **重大度**: 🟡 Warning（コード経路は概ね正常。到達/トランスポート要因が濃厚）
- **報告症状**: 不具合3「メールが届かない」
- **該当**: `includes/class-email.php`, `includes/class-integrations.php`, `includes/class-reservation-context.php`

### 調査結果（コード経路は機能的に正常）
フロント予約（`class-rest-public.php:996`）・管理画面の手動予約（`class-rest-reservations.php:391`）の両方で `smart_booking_reservation_received` が発火し `send_receipt()` が呼ばれる。テンプレート初期値も有効化時に投入済み。したがって主因はコードではなく**送信・到達レベル**の可能性が高い。候補を切り分ける。

### 候補原因と再現条件
1. **トランスポート未設定（最有力）**: `wp_mail()` は PHP `mail()`／MTA 依存。MTA/SMTP 未設定のサーバ（新規 VPS 等）では**無言で捨てられる**。しかも `class-email.php` は `wp_mail()` の戻り値（false）を**握り潰し、ログも残さない**設計 → 失敗が見えない。
2. **到達性（Gmail 等）**: 差出人は既定 `admin_email`。これが `@gmail.com` や独自ドメインで、サーバIPから直送すると SPF/DKIM/DMARC 不整合で**拒否・迷惑メール送り**。
3. **条件付きの管理者通知**: `smart_booking_mail_admin_notify_enabled` が OFF かつ店舗メール未設定だと管理者宛は無言で送信されない。件名か本文テンプレートが空でも無言スキップ。
4. **宛先バリデーション**: `store_email` / `staff_email` が `is_email()` を通らない値だと除外される。

### 修正・対応方針
- **コード側（backend-generator）**: 送信失敗の可視化。`error_log` は審査 NG のため、`wp_mail_failed` フックで直近の失敗を transient/option に記録し、設定画面「メール通知」タブに「直近の送信に失敗しました」の注意を出す案（審査安全な範囲で）。診断性が上がり、以後の「届かない」を切り分け可能にする。
- **環境側（人間対応）**: SMTP 送信の導入（WP Mail SMTP 等）＋ 差出人ドメインの SPF/DKIM/DMARC 整備。ローカル検証では MailHog 等で To/Cc/Subject/本文を確認する。
- logic-evaluator は「`wp_mail` が正しい引数で呼ばれるか」までをゲート対象とし、**実到達性は環境要因として分離報告**する。

### 検証（logic-evaluator）
- wp-env で予約作成 → `wp_mail` の呼び出し引数（To/Cc/Subject/本文の変数展開）を検証。
- 管理者通知トグル ON/OFF・店舗メール有無・担当者CC の分岐を網羅。

---

## （継続）BUG-A: REST パーマリンク依存（v0.2.3 で併せて対応推奨）

- **重大度**: 🔴 高（初回利用者が高確率で遭遇）／引き継ぎドキュメント記載の既知バグ
- **症状**: パーマリンク=「基本（Plain）」のとき、React が使うハードコード `/wp-json/...` が 404 相当になり、管理画面スケジュール等でルート不一致エラー。**フロント予約フォームも同依存なら Plain 環境で予約自体が動かない可能性**。
- **不具合2との関係**: Plain パーマリンク環境では POST /schedules が 404 → 保存失敗。SchedulePage はエラートーストを出すため「完全にサイレント」ではないが、環境によっては「登録できていない」の別経路になり得る。logic-evaluator は再現時にこの条件も切り分ける。
- **修正方針**: REST URL を `esc_url_raw(rest_url())` ベースに統一（admin・frontend 両系統）。`grep -rn "wp-json" src/ includes/` で洗い出し。

## （継続）BUG-B: 管理画面ロゴが配布ZIPに未同梱

- **重大度**: 🔵 中（見た目のみ）
- **症状**: `src/admin/App.jsx` が `docs/images/SmartBookingLogo.svg` を参照。`docs/` は配布ZIP除外 → 画像割れ。
- **修正方針**: ロゴを配布対象（`build/images/` 推奨）へ移動し参照を修正。ZIP同梱を検証。

---

## BUG-4（構造対策）: schedules テーブルに UNIQUE 制約がない

- **重大度**: 🟡 Warning（不具合1の「不安定」を底上げする構造要因）
- **該当**: `includes/class-activator.php` の `smart_booking_schedules`（`idx_store_staff_date` は非ユニーク KEY のみ）
- **内容**: `(store_id, staff_id, schedule_date, start_time)` に UNIQUE 制約がなく、重複防止がアプリ層の read-then-write に依存。連打・並行送信で重複行ができ得る。
- **修正方針**: dbDelta で UNIQUE 制約を追加（＋ `smart_booking_db_version` bump ＋ 追加前の既存重複解消手順）。`create_item` / `copy_schedules` は制約違反時のハンドリングを持たせる。BUG-1/2 と同時に入れると効果的。

---

## 対応順序（planner 推奨）
1. **BUG-1/2**（最優先・データ損失）＋ **BUG-4**（同時に構造対策）。隔離ゲートで証明。
2. **BUG-A**（Plain パーマリンク。初回利用者影響大）。
3. **BUG-3**（コード側は診断可視化、到達は環境対応）。
4. **BUG-B**（見た目、リリースついでに）。

各修正後に回帰ゲート（既存スイート ＋ Plugin Check 0/0）。全部入れてから 0.2.3 リリース手順（バージョン4箇所・Changelog・ZIP・SVN commit）は**人間 GO**。

---

## 進捗更新 2026-07-12: BUG-1/2 ＋ BUG-4 修正完了（全ゲート Green・未リリース）

**ステータス**: 🟢 修正済み・検証済み（コードは未リリース。リリース手順＝バージョン更新以降は人間 GO 待ち）。運用ループ: 再現ファースト（Red 7件）→ 修正 → 全ゲート Green で1サイクル完了（再修正0回）。

### 変更ファイル（2つのみ・公開契約非破壊）
- `includes/rest/class-rest-schedules.php` `copy_schedules()`: 既存チェック・上書き DELETE・カウントを、コピー元に含まれる (store_id,staff_id) 集合の各スコープに閉じた（日付一括 COUNT/DELETE を撤去）。上書きは対象 (store_id,staff_id,schedule_date) かつ booked_count=0 のみに限定、予約済み枠は保護。再挿入は `INSERT ... ON DUPLICATE KEY UPDATE`（booked_count 非変更、capacity は GREATEST で下げない）。req/res 形不変。
- `includes/class-activator.php`: dbDelta の CREATE TABLE に `UNIQUE KEY uniq_store_staff_date_time (store_id,staff_id,schedule_date,start_time)` 追記（新規インストールでも付与）。maybe_upgrade（run_migrations → migrate_schedules_unique_index）で「①dedup（victim の reservations.schedule_id を survivor へ UPDATE 張り替え → survivor の booked_count を `status!='cancelled'` COUNT で再計算 → capacity=max(グループ内最大, 再計算)）→ ②明示 `ALTER TABLE ... ADD UNIQUE KEY` → ③`SHOW INDEX` で実在検証 → ④**検証成功時のみ `smart_booking_db_version` を 0.2.3 へ bump**（失敗時は据え置き・後退なし）」。survivor 選定=非キャンセル予約を持つ最小 id 優先 → 無ければ最小 id（option 1）。冪等（UNIQUE 既存なら no-op）。

### ゲート結果（logic-evaluator, すべて Green）
- **A 回帰**: 再現テスト `tests/red/bug124-red.php` = `PASS=7 FAIL=0`（Red→Green 反転）、`tests/e2e/bug124-scope-isolation.spec.js` = passed。既存 phase1〜3 = Green（唯一の赤は wp-env 一過性接続 flake、単体再実行で phase3-flow 13/13 Green）。Plugin Check はプロダクトコード 0 errors / 0 warnings（残余指摘は全て非配布ファイル由来＝`.distignore` で配布ZIP除外）。
- **B 店舗×担当者スコープ隔離**: 上書きON で他店舗/他担当者の未予約枠が生存（expected=1 actual=1）、上書きOFF で対象店舗の枠が作成（inserted=1）、単一店舗×担当者2名でも他担当者の枠を保護。
- **C 補強1（孤児化禁止・整合）**: 移行プローブ PASS=15 FAIL=0。孤児予約ゼロ、survivor booked_count == 非キャンセル予約数、全 schedules 行で capacity>=booked_count、survivor=option1。
- **D 補強2（UNIQUE 実在・発火・失敗時 bump なし）**: UNIQUE 実在・重複 INSERT 拒否、有効化フック経路で dedup→ALTER→検証が発火、冪等（2回目 no-op・db_version 後退なし）、失敗時は db_version 据え置き（自動更新シミュレーション実測＋確定ロジックのコード確認の二重で担保）。
- **E 契約非破壊**: copy_schedules の req（source_date/target_dates/store_id/staff_id/overwrite）/res（inserted/skipped/overwritten）形不変。DB 既存カラム不変（追加は UNIQUE インデックスのみ）。

### 未決（人間判断待ち）
- **自動更新ギャップ**: 移行は有効化フック経路のため、自動更新では再有効化まで走らない（copy スコープ修正=コードは自動更新でも即時有効）。二択: (a) 現状維持（有効化時移行）／(b) `smart-booking.php` にバージョンゲート付き upgrade フック追加で自動更新でも発火。(b) は今回 GO のファイル範囲外＝人間承認後に小委譲＋再ゲート。

### 申し送り（本修正スコープ外）
- readme.txt 英語readme言語 ERROR 2件（WordPress.org 新規則）。配布判断時に別途対応。
- `tests/red/_tmp_migration_probe.php` 残置（配布対象外だが掃除候補）。

---

## 進捗更新 2026-07-12（第2報）: 自動更新フック(b) 実装完了・再ゲート Green ／ phase3 既存失敗を切り分け

### 人間承認: 自動更新ギャップ = (b) 採用
UNIQUE＋dedup 移行を、プラグイン自動更新（有効化フック非発火）でも一度だけ発火させる。今回のこの作業に限り承認ファイル範囲を `smart-booking.php` に拡張。

### 変更（追加分・公開契約非破壊）
- `includes/class-activator.php`: `public static function maybe_upgrade()` 追加。中身は `if ( version_compare( (string) get_option('smart_booking_db_version','0.0.0'), '0.2.3', '<' ) ) { self::run_migrations(); }` のみ（既存 run_migrations を再利用・ロジック二重化なし）。★ゲート値は schema 移行対象 `'0.2.3'`（`SMART_BOOKING_VERSION` は 0.2.2 のまま＝これでゲートすると既存 0.2.2 ユーザーで発火しないため使わない）。
- `smart-booking.php`: `add_action('admin_init', array('Smart_Booking_Activator','maybe_upgrade'));`（無条件実行でない・`init` 無条件テーブル作成でない）。`SMART_BOOKING_VERSION` は不変。

### 再ゲート結果（logic-evaluator）— 本ワークパッケージ固有は全 Green
- A1 再現テスト Green 維持（bug124-red FAIL=0 / 隔離 spec passed）。
- A3 Plugin Check: 変更2ファイル（`smart-booking.php` / `class-activator.php`）0/0。残余は非配布ファイル由来。
- B 隔離ゲート Green。C 補強1 Green（孤児0／booked_count 整合／capacity>=booked_count／survivor=option1）。D 補強2 Green（UNIQUE 実在・重複 INSERT 拒否・冪等・失敗時 bump なし）。
- **E 自動更新シナリオ（最重要・実測 PASS=18/FAIL=0）**: UNIQUE を DROP＋db_version を 0.2.2 へ戻し重複行＋予約を投入した「自動更新直後・未移行」状態から、**activate() を呼ばず maybe_upgrade() を1回**呼ぶだけで dedup＋UNIQUE 完了・db_version→0.2.3・孤児0・booked_count 整合。2回目 no-op（冪等）。db_version=0.2.3 状態では version_compare により run_migrations 未呼出＝ゲート閉（1回だけ発火の裏付け）。
- F 契約非破壊: copy_schedules req/res 不変・DB 既存カラム不変。

### 回帰ゲート全体（phase1〜3）: A2 のみ未達 — ただし本変更起因ではない（decisive）
- phase1＋phase2 = 126 passed / 1 skipped（Green）。phase3 に **3件恒常失敗**: `phase3-validation.spec.js:115` / `phase3-fix1.spec.js:45` / `phase3-responsive.spec.js:967`。
- **ベースライン切り分け**: 我々の PHP 3変更を `git stash` で退避（公開 v0.2.2 ロジック）して同3specを再実行 → **変更あり／ベースラインで同一アサートが同一に失敗**（両方 3 failed / 19 passed）。`build/`（フロントバンドル）不変。作業ツリーは md5 一致で原状復帰確認済み。→ **ケースY＝本ワークパッケージ起因の回帰ではない**。
- 根本原因（推定）: テスト期待が現行フロント設計と乖離。現行は `src/frontend/App.jsx:155-167` の単一ページ `MainInputPage`（日付＋時間＋入力を1画面統合、独立見出し「お客様情報の入力」ステップ無し）。3テストは多段フロー／print CSS 前提で古い。
- **別課題として切り出し**（判断二択）: (A) 古いテストを現行設計に合わせ更新／(B) 多段フローが仕様書 3.5 で正なら現行フロントが仕様乖離＝frontend 調査。→ 仕様書 3.5 / MainInputPage 設計意図の確認が必要。planner は本 phase3 失敗で BUG-1/2/4＋自動更新の再ゲートをブロックしない（回帰ではないため）が、v0.2.3 リリースはこの決着まで保留。

### ステータス
🟢 BUG-1/2・BUG-4・自動更新フック(b): 実装完了・固有ゲート全 Green（未リリース）。
🟡 phase3 3テスト vs 現行フロント設計: 既存乖離・人間判断待ち（本修正スコープ外）。

---

## 進捗更新 2026-07-12（第3報・クローズ）: (C) で作業パッケージ確定

### 決定（人間 (C) 選択）
BUG-1/2・BUG-4・自動更新フック(b) を **実装完了・全固有ゲート Green・未リリース**でクローズ。以降はコード無改修（ドキュメント／ゲート定義／記録のみ）。

### phase3 赤3件 = 別件化
本変更起因でない（`git stash` ベースライン同値で証明済み。両方 3 failed / 19 passed・build 不変・md5 一致で原状復帰）。仕様 vs 出荷済み設計の乖離として独立トラックに切り出し。正本: `docs/bugs/spec-vs-shipped-booking-flow.md`。該当: `phase3-validation.spec.js:115` / `phase3-fix1.spec.js:45` / `phase3-responsive.spec.js:967`。BUG-A/3/B とは独立、今回決着しない。

### 回帰ゲート定義の改訂
「既存 phase1〜3 全 Green」→「ベースライン差分で変更起因の新規失敗ゼロ」。決定の正本: `docs/decisions/0001-regression-gate-baseline-diff.md`（stash 切り分け手続きを正式化）。CLAUDE.md ＋ `.claude/agents/logic-evaluator.md` への実反映は**ユーザー確認待ち**（設定/CLAUDE.md 変更はエージェント指示だけで行わない方針。提案差分は decision に記載）。既存条件（再現テスト Green ＋ Plugin Check 0/0 ＋ 隔離ゲート Green）は維持。

### 変更ファイル（未コミット・未リリース）
- `includes/rest/class-rest-schedules.php`
- `includes/class-activator.php`
- `smart-booking.php`

### 次アクション
- 残バグ BUG-A / BUG-3 / BUG-B（別途着手指示待ち）。
- phase3 乖離のプロダクト判断（`docs/bugs/spec-vs-shipped-booking-flow.md`）。
- リリースは全件決着後に人間 GO。

---

## 進捗更新 2026-07-13（第4報・クローズ）: BUG-A（REST パーマリンク依存）実装完了・全ゲート Green

### 根本原因（実測確定）
Plain パーマリンクでは `rest_url('smart-booking/v1/')` が `http://<host>/index.php?rest_route=/smart-booking/v1/`（クエリ文字列形）を返す。両 api.js の `buildUrl()` が末尾で `return qs ? base + '?' + qs : base;` と**無条件に `?` を付与**するため、base に既に `?rest_route=` がある Plain 環境で**二重 `?`**（例 `...?rest_route=/smart-booking/v1/schedules?store_id=1`）となり `rest_route` 値が壊れて **404 `rest_no_route`**。パラメータ無し呼び出しは通るため「一部だけ失敗＝不安定」。pretty では正常。PHP（localize）は既に正しく、フロントのみの不具合。

### 変更（未コミット・未リリース・公開契約非破壊）
- `src/admin/api.js` / `src/frontend/api.js` の `buildUrl()`: `const sep = base.indexOf('?')===-1 ? '?' : '&'; return base + sep + qs;`（既存クエリがあれば `&`、無ければ `?`）。ハードコード `'/wp-json/smart-booking/v1/'` フォールバックを除去（`globalCtx.restUrl || ''` ＋不在時 console.warn）。`npm run build` エラー0で `build/*` 反映。PHP・REST 契約・`SMART_BOOKING_VERSION` 不変。

### ゲート結果（全 Green）
- **A Plain 再ゲート（logic-evaluator, フォアグラウンド）**: 正ゲート spec `tests/e2e/bug-a-plain-regate.spec.js` = **2 passed**。Plain で LIVE restUrl・実バンドルリクエストとも `.../index.php?rest_route=/smart-booking/v1/public/staff&store_id=2` 等で **HTTP 200・単一 `?`・二重 `?` 不在・`rest_no_route` ゼロ**。
- **B 回帰（ベースライン差分・decision 0001）**: pretty で phase1〜3 = **179 passed / 3 failed（既知 stale: `phase3-fix1.spec.js:45` / `phase3-responsive.spec.js:967` / `phase3-validation.spec.js:115`）/ 1 skipped / 22 did not run**＝**新規失敗ゼロ**。加えて pretty では base に `?` 無し→`sep='?'`＝旧実装と同一 URL の解析論拠で挙動不変を担保。
- **C 契約非破壊**: エンドポイントパス（`schedules`/`public/staff`/`public/availability`/`stores`/`reservations`）・パラメータ名・req/res 形不変（URL セパレータのみ変更）。
- **ux（ux-evaluator, Plain 実挙動）**: Plain 環境でフロント予約フォームが「読み込み→枠取得→入力→確認→完了(#364)」まで全 200・404/エラートースト無しで完走。BUG-A の実害解消を実機確認。permalink を pretty へ復元・テストデータ削除済み。

### spec 運用の是正（取り違え防止）
- Red 実証専用 `tests/e2e/bug-a-plain-repro.spec.js`（buildUrl を無条件 `?` 前置で再実装＝修正後も設計上ずっと赤）を **`test.describe.skip`＋ヘッダ注記**に変更し、常時ゲートから除外。**正の Green ゲートは `tests/e2e/bug-a-plain-regate.spec.js`**。今後この repro spec の赤を回帰失敗と誤認しない。

### 経緯メモ（1行）
- Step3 実測を detached background で回したジョブ `bqrbr8od8` は正常完了（回帰 part B）していたが、planner が結果回収前に停止し約半日孤児化。かつ Green ゲートに Red 実証専用 spec を取り違えていた。→ 完了済み part B を回収採用＋正ゲート `regate` spec をフォアグラウンド実行して是正・確定。以後 evaluator の長時間スイートは detached を避けフォアグラウンド＋タイムアウトで回す。

### 付記（新規・BUG-A 非関連の別件）
- **「残りわずか(few_left)」の視覚表現が無効化**: `src/frontend/styles/frontend.css` の相反ルール（`:794` の警告背景を `:2305` が白で上書き＋`:2360` でテキストバッジ非表示）により、`available` と `few_left` が見た目で完全同一（aria-label のみ残存）。日付カード側 `is-tone-few` も対応 CSS 無し。出荷済み CSS のデグレ疑い＝要プロダクト判断・frontend 切り出し推奨。

### ステータス
🟢 BUG-A: 実装完了・Plain/pretty 両動作 Green・回帰新規失敗ゼロ・契約非破壊・**未リリース**でクローズ。
🟡 few_left 視覚表現: 別トラック・人間判断待ち。

### 次アクション
- 残バグ BUG-3 / BUG-B（別途着手指示待ち）。phase3 乖離・few_left のプロダクト判断。リリースは全件決着後に人間 GO。

---

## 進捗更新 2026-07-13（第5報）: BUG-3（メール未達）Step 1 調査・再現 完了 → スコープ選択待ち（実装未着手）

運用: 調査ファースト。logic-evaluator がローカル wp-env で実測（フォアグラウンド＋タイムアウト・detached 不使用）。**実装は人間のスコープ選択後**。

### 切り分け結論（主因）
**主因はコードではなく送信/到達（環境要因）が最有力**。テンプレートが投入済み（＝真のフレッシュ有効化）なら、受付・承認とも `wp_mail` は**正しい To/Cc/From/件名/本文（変数展開込み）で確実に呼ばれる**＝コード経路は機能的に正常。一方で **失敗が一切可視化されない**構造（診断性の欠如）が「届かない」を悪化させている。

### 実測（A: wp_mail 呼び出し）
- 受付ユーザー宛・受付管理者宛（To=admin+store, Cc=staff）・承認ユーザー宛が正しい宛先/件名/本文で発火。`{customer_name}`/`{schedule_date}`/`{schedule_time}`/`{store_name}`/`{staff_name}`/`{reservation_id}` の**変数展開は全て成功**。

### 分岐マトリクス（B・無言 skip の実在）
- 正常: 管理ON×店舗有×担当有／管理ON×店舗無（To=admin,Cc=staff）／管理OFF×店舗有（To=store）／不正 is_email 除外。
- **無言 skip（コード/設定起因・失敗が見えない）**:
  - 管理OFF×店舗無 → 管理者通知を無言 skip（`class-email.php:85`）。
  - ユーザー/管理者/承認テンプレ**空** → 該当メールを無言 skip（`:173`）。
  - customer_email 不正 → ユーザー宛を無言 skip（`:166`）。

### 失敗の握り潰し（C）
- `class-email.php:180` が `wp_mail()` の戻り値を捕捉せず、`wp_mail_failed` も未購読。送信失敗（false）が **option/transient/画面/ログのどこにも記録されない**（コメント L12「送信失敗はサイレント無視・error_log 不使用」）。→ ユーザーが未達に気付けない。

### 環境（到達性・人間対応事項）
- ローカル wp-env は実トランスポート停止（MailPit 停止・SMTP 接続不可 → 実 `wp_mail` は即 FALSE、外部へ出ない）。本番でも MTA 未設定/SPF・DKIM・DMARC 不整合なら同様に無言で落ちる。差出人 From 既定は `admin_email`（`build_headers` L193-195）。
- 注記: 現 wp-env は `smart_booking_` オプションが4件のみでメールテンプレ6種が欠落（過去の uninstall 検証で消えた**開発環境アーティファクト**。activator は `add_option` で非空既定をシードするのでフレッシュ有効化なら非空）。ただしユーザーがテンプレ欄を空保存すれば同じ無言 skip に到達。

### スコープ選択肢（実装は人間選択後・未着手）
- **(i) コード側＝送信失敗の可視化**: `wp_mail_failed` を購読し直近失敗を transient/option に記録 → 設定「メール通知」タブに「直近の送信に失敗しました」注意表示。任意でテンプレ空/宛先不正の無言 skip も可視化。**UI 追加を伴い要人間承認**。コスト=backend（フック＋記録）＋frontend（設定タブ注意 UI）＋ux-evaluator。審査安全=可（transient/option・出力エスケープ・nonce+cap、error_log/外部通信なし）。
- **(ii) 運用側＝ドキュメント整備**: SMTP 送信（WP Mail SMTP 等）＋差出人ドメイン SPF/DKIM/DMARC 手順を docs 化。コード変更なし。主因（到達性）に直接効く。審査影響なし。
- **(iii) 両方**（診断性回復＋主因対処）。

planner 推奨: 主因が到達性のため **(iii)**。(i) は UI 追加のため承認後に着手。(ii) は docs のみ。いずれも本 Step2 では未実装（人間選択待ち）。

---

## 進捗更新 2026-07-13（第6報・クローズ）: BUG-3 (iii) 実装完了・全ゲート Green（未リリース）

人間が (iii) 両方を承認 → (i) 失敗の可視化＋(ii) docs 整備を実装。コード経路は正常（主因は到達性＝環境要因）を前提に、診断性の回復＋運用ガイドで対処。

### (i) 送信失敗の可視化 — 実装
- backend `includes/class-email.php`: `send()` で wp_mail 直前に一時 `wp_mail_failed` リスナ add→wp_mail→remove（自身の送信にスコープ）、L180 戻り値捕捉。失敗/skip を transient `smart_booking_last_mail_error`={time,category,reason,to_type} に記録。category=`transport_failed`/`skipped_empty_template`(:173)/`skipped_invalid_recipient`(:166)。**成功時クリア**（誤検知防止）。T3(:85) は対象外（意図的無送信）。
- backend `includes/rest/class-rest-settings.php`: 新規 `GET /smart-booking/v1/mail-error`→`{error:{...}|null}`、`DELETE`（dismiss）。nonce＋`manage_options`。**既存 `/settings` は不変**。
- frontend `src/admin/api.js`（`mailError.get/clear`）／`src/admin/pages/settings/MailSettingsTab.jsx`（記録ありのとき警告バナー＋「閉じる」・category→日本語・時刻/宛先併記）／`admin.scss`（`.smb-alert--warning`）。build 反映・バージョン不変。

### (ii) ドキュメント整備 — 完了
- `docs/ops/email-deliverability.md`（SMTP 導入＋差出人ドメイン SPF/DKIM/DMARC の運用ガイド。主因＝到達性に直接効く。コード変更なし）。readme.txt の通信記述と矛盾しない範囲。

### ゲート結果（全 Green）
- **A Red→Green（失敗可視化）**: green-verify probe `PASS=18 FAIL=0`。transient 記録／`GET /mail-error` error!=null／成功時クリア／T1・T2 記録／T3 非記録／DELETE dismiss／認可 401（GET・DELETE とも manage_options 必須）。
- **B 回帰（ベースライン差分・新規失敗ゼロ）**: 影響領域をフォアグラウンド・チャンク実測 — phase4-email=5 passed（メール経路直撃・変数render/Cc/承認件名/店舗メール空フォールバック/From空）、phase2-settings＋phase2-reservations＋smoke=43 passed（メール通知タブ含む5タブ・REST契約・予約作成）、phase3-flow passed。既知 stale（`phase3-fix1:45`/`phase3-validation:115`/`phase3-responsive:967`）以外の**新規失敗ゼロ**。未実測領域は send() 経路・MailSettingsTab への局所性で補完。
- **C Plugin Check**: 変更ファイル（class-email.php / class-rest-settings.php / src/admin/*）指摘0。
- **D 契約非破壊**: `/settings` GET/POST 形不変（29キー・漏洩なし）、`/mail-error` は追加のみ、DB カラム不変。
- **ux（ux-evaluator）**: 3 category ともバナー表示・日本語自然・時刻/宛先明快・閉じる動作＋永続・記録なし非表示（誤検知なし）・375px 崩れなし・既存タブ操作と共存。全 Green。

### 検証資産（未追跡・回帰 glob 外）
- `tests/red/bug3-mail-failure-red.php`（Red 実証・`/settings` 版）、`tests/red/bug3-mail-green-verify.php`（Green 検証・`/mail-error` 版）。Step3 再実行に使用可。

### 非ブロッキングの改善提案（任意・ux-evaluator）
- `skipped_invalid_recipient` を `to_type` 別に誘導文出し分け（user=予約者入力ミス／admin=店舗・担当者メール設定要確認）。
- `transport_failed` の「SMTP」表現をもう一段具体化（WP Mail SMTP 導入等の次の一歩）。
→ 今回は非対応（YAGNI／別途判断）。

### ステータス
🟢 BUG-3 (i) 可視化: 実装完了・全ゲート Green・**未リリース**。🟢 (ii) docs: 完了。主因（到達性）は環境側対処＝人間対応（docs 提供済み）。

### 次アクション
- 残バグ BUG-B（ロゴ同梱）。phase3 乖離・few_left・ゲート定義 CLAUDE.md/logic-evaluator.md 反映（ユーザー確認）。リリースは全件決着後に人間 GO。

---

## 進捗更新 2026-07-13（第7報）: BUG-B（管理画面ロゴ未同梱）Step1 再現・切り分け完了 → スコープ承認待ち（実装未着手）

### 根本原因（プロダクト起因で確定）
- `src/admin/App.jsx:27` が管理画面ヘッダのロゴを**実行時 URL**で参照: `logoSrc = ${pluginUrl}docs/images/SmartBookingLogo.svg`（webpack import ではないため build に emit されない。`build/admin.js` にこの文字列がハードコードされているのを実測確認）。
- ロゴ実体は `docs/images/SmartBookingLogo.svg` にのみ存在。`.distignore` は `docs/`・`src/` を除外し **`build/` のみ配布**。`build/images/` は存在しない。
- → 配布 ZIP に `docs/` が含まれず、配布環境で `{pluginUrl}docs/images/SmartBookingLogo.svg` が 404＝**ロゴ割れ**。dev（wp-env bind mount）は `docs/` が在るため表示される＝**偽陰性**。
- 契約（REST req/res・DB カラム）・店舗×担当者スコープ隔離への波及**なし**（純粋な静的アセットのパス問題）。

### Step1 Red（logic-evaluator）
- `tests/e2e/bug-b-logo-shipping.spec.js`: 描画 img src=`.../docs/images/SmartBookingLogo.svg`（naturalWidth=574＝dev 偽陰性を明示）／`.distignore` 適用の配布セットにロゴ**不在＝Red**（`plugin-zip` 不使用）。`build/admin.js` は配布される（配布可能な代替あり）。

### 修正方針の選択肢（実装は人間承認後）
前提: `build/` は gitignore かつ配布対象。手置き `build/images/` は git 管理外＋`npm run build` で消え得るため、**webpack が build/ へ emit する構成が必須**。
- **(A) webpack import 化【推奨】**: ロゴを src 配下（例 `src/admin/images/SmartBookingLogo.svg`）へ置き、App.jsx で `import logoUrl from './images/SmartBookingLogo.svg'`（@wordpress/scripts の SVG 既定＝default export は URL）。webpack が build へ emit・publicPath auto で解決。**新規依存なし・.distignore 変更なし**。コスト小。契約影響なし・UI 追加なし・審査安全（バンドル同梱・CDN 不使用）。
- **(B) CopyWebpackPlugin で build/images/ へコピー**: `copy-webpack-plugin`（新規 devDep）を webpack.config.js に追加し src のロゴを `build/images/` へコピー、App.jsx は `${pluginUrl}build/images/...` 参照（既存の実行時 URL 方式を踏襲・固定パス）。新規依存追加の分だけ表面積が増える。契約/UI/審査は (A) と同等。
- 触るファイル候補: `src/admin/App.jsx`＋アセット配置（src へ移動/複製）＋（B のみ webpack.config.js/package.json）。`docs/images/` の実体は docs 参照が他にある可能性を考え**複製**が無難（移動より低リスク）。
- Step3 検証: `bug-b-logo-shipping.spec.js` の期待パスを修正先へ更新→Green／回帰＝ベースライン差分で新規失敗ゼロ／Plugin Check 0/0／ux でロゴ表示確認。

### ステータス
🔵 BUG-B: Step1 再現・切り分け完了。**実装は (A)/(B) の人間承認後**。契約非破壊・UI 追加なし（既存ロゴのパス修正）。

---

## 進捗更新 2026-07-13（第8報・クローズ）: BUG-B（管理画面ロゴ未同梱）(A) webpack import 化 実装完了・全ゲート Green（未リリース）

### 実装（人間承認: (A) webpack import 化）
- `src/admin/images/SmartBookingLogo.svg` を新規追加（`docs/images/` の実体は残置）。
- `src/admin/App.jsx`: `import logoSrc from './images/SmartBookingLogo.svg'` に変更し、実行時 `${pluginUrl}docs/images/...` 参照を廃止。`<img class="smb-app__brand-logo" alt="Smart Booking" src={logoSrc}>`。
- **採用機構＝インライン base64 data URI**: `@wordpress/scripts`（`@svgr/webpack`＋`url-loader`）が 785B の SVG を `build/admin.js` に data URI としてインライン同梱（別ファイル emit ではない）。`.distignore`／`webpack.config.js` 未変更。build エラー0・lint 0・バージョン不変。

### ゲート結果（全 Green）
- **A 配布ZIP実行時解決（実生成→展開・検証目的／リリースではない）**: ZIP に `docs/` 不在、`build/admin.js` に `data:image/svg+xml` 同梱、admin.js が wp-env build と **md5 一致**（`7aad95a0ec56c889360069a07589bd3f`）、admin.js に `docs/images` 参照ゼロ、管理画面で img src=data URI・**naturalWidth=574**（desktop/mobile 描画）＝data URI self-resolve で 404 不能。Red 4→Green 6 反転（`tests/e2e/bug-b-logo-shipping.spec.js` を新機構期待へ更新）。
- **B 回帰（ベースライン差分・新規失敗ゼロ）**: phase2-pages-render 14 passed（コンソールエラー0＝ロゴ404なし）、phase8-ui-ux-fixes 16 passed。既知 stale 3件以外の新規失敗ゼロ。ロゴ import はヘッダ描画に局所化。
- **C Plugin Check**: 変更/配布ファイル（App.jsx・新規 svg・build/admin.js）0/0。
- **D 契約非破壊**: 静的アセットのみ・REST/DB 非関与・`SMART_BOOKING_VERSION` 不変。
- **ux（ux-evaluator）**: desktop/mobile でロゴ割れず表示・サイズ/配置自然・alt あり。Green。

### 非ブロッキング改善提案（任意・ux-evaluator）
- img の `alt="Smart Booking"` と親 `div.smb-app__brand` の `aria-label="Smart Booking"` が二重発話の可能性。装飾なら img を `alt=""`+`aria-hidden` にするか親 aria-label を外すか統一（任意・YAGNI）。

### ステータス
🟢 BUG-B: 実装完了・配布ZIP実行時解決まで Green・**未リリース**でクローズ。

### v0.2.3 対象バグの完了状況
- BUG-1/2・BUG-4・自動更新(b)（第1〜3報）／BUG-A（第4報）／BUG-3 (i)+(ii)（第5〜6報）／BUG-B（第8報）＝**全てクローズ・未リリース**。
- 次は v0.2.3 リリース手順（人間 GO）。ただし別トラック（phase3 仕様 vs 出荷乖離／few_left 視覚表現／BUG-3 UX 改善／BUG-B aria-label／ゲート定義の CLAUDE.md・logic-evaluator.md 反映）はリリース前に人間判断が要るものを整理のこと。

---

## 進捗更新 2026-07-13（第9報）: readme.txt 英語readme ERROR 2件 解消（ローカルコミット・未push）
- WordPress.org 2025-07-28 ポリシー対応。短い説明（11行目・130字）と `== Description ==`（13〜55行）を日本語化前の元英語（`e59b3f5`）へ復元。他セクション（Installation/FAQ/Changelog 等）は日本語維持。**Stable tag 0.2.2 据え置き・他ファイル変更なし＝リリースではない**。
- Plugin Check 実測（独立再実行）: `readme_short_description_non_official_language` / `readme_description_non_official_language` = **0 件**（`FILE: readme.txt` ブロック消滅）、readme 系の新規 ERROR/WARNING なし。復元英語 Description に削除機能・Pro/有料記述なし。
- ローカルコミット: ブランチ `fix/v0.2.3-bugfixes` / コミット `47ed8ec`（readme.txt のみ・27/27）。**push・SVN は未実施（人間判断）**。

---

## 進捗更新 2026-07-13（第10報・クローズ）: few_left（残りわずか）視覚回帰 修正完了・仕様 3.4 準拠・全ゲート Green（未リリース）

### 症状/根本原因
仕様 3.4「残りわずか＝選択可能（警告色）」に反し、`frontend.css` の相反ルールで few_left が available と視覚区別不能だった（`:794` の警告色を `:2305` の通常色上書きが後勝ちで打ち消し＋`:2361` バッジ `display:none`＋日付側 `.smb-front-day-tile.is-tone-few` の CSS 不在）。ledger 第4報の申し送りを消化。

### 修正（CSS のみ・JSX/ロジック不変）— `src/frontend/styles/frontend.css`
- 時間枠 `.smb-front-time-btn.is-few_left` を警告色に是正（`background:#fffbeb; border-color:#fbbf24`、hover `#fef3c7`）。
- `.smb-front-time-btn__badge.is-few_left` を可視化（few_left のみ・他状態のグローバル非表示は維持）。
- `.smb-front-day-tile.is-tone-few { background:#fef3c7 }` 追加（選択可能のまま）＋ `.is-tone-few.is-selected` 優先ルール。
- available/full/closed は不変。

### ゲート結果（全 Green）
- **A Red→Green**: `tests/e2e/few-left-visual-repro.spec.js` が desktop/mobile とも **3 passed**。4状態 computed 実測: available bg `rgb(255,255,255)`／few_left bg `rgb(255,251,235)`#fffbeb・border `rgb(251,191,36)`#fbbf24・バッジ display=flex／full・closed グレー`rgb(248,249,250)`・disabled。日付タイル few_left `rgb(254,243,199)`#fef3c7。few_left 押下可能・full/closed disabled。
- **B 回帰（新規失敗ゼロ）**: phase9-redesign-style 全 Green（selected 状態・トークン）、phase9-redesign-picker 7 passed（トグル・満席 disabled 含む／初回1失敗は wp-cli 接続 flake→単体再実行 Green）、phase3-validation は既知 stale `:115` のみ、phase2-pages-render 7 passed（コンソールエラー0）。
- **C Plugin Check**: frontend.css 指摘0。**D 契約非破壊**（CSS のみ・REST/DB 非関与）。
- **ux（ux-evaluator）**: desktop/375px で4状態（available=白/few_left=#fffbeb警告+バッジ/full=灰「満席×」/closed=灰「締切×」）の色区別＋押下可否（available/few_left 押下可・full/closed 不可）とも正しく伝わる。色弱配慮のテキスト併記あり。Green。

### 非ブロッキング提案（任意）
- full/closed のアイコン差別化（色以外の区別強化）／日付ストリップ横スクロールの気づきやすさ（別件・スコープ外）。

### ステータス
🟢 few_left 視覚回帰: 修正完了・仕様 3.4 準拠・全ゲート Green・**未リリース**。変更＝`src/frontend/styles/frontend.css`（未コミット）＋新規 `tests/e2e/few-left-visual-repro.spec.js`。
